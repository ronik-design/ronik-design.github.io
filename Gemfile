source 'https://rubygems.org'

# GitHub Pages gem
# gem 'github-pages', group: :jekyll_plugins

gem 'jekyll', '~> 3.2', '>= 3.2.1'

# HTML Proofer, for simple tests
gem 'html-proofer'

# If you want to use plugins not supported by GitHub Pages
# add theme here, but be warned, they will not work in the GitHub Pages
# hosting environment
group :jekyll_plugins do
  # gem 'jekyll-github-metadata'
  gem 'jekyll-'
  gem 'jekyll-mentions'
  gem 'jemoji'
  gem 'jekyll-redirect-from'
  gem 'jekyll-sitemap'
  gem 'jekyll-feed'
  gem 'jekyll-paginate'
  gem 'jekyll-gist'
  gem 'jekyll-seo-tag'
end
